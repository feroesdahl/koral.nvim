local colors = require("koral.colors")

return {
  normal = {
    a = { bg = colors.cyan, fg = colors.black },
    b = { bg = colors.dim_grey, fg = colors.orange },
    c = { fg = colors.white },
    x = { fg = colors.purple },
    y = { bg = colors.dim_grey, fg = colors.blue },
    z = { bg = colors.cyan, fg = colors.black },
  },
}
